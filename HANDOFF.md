# HANDOFF — RAORA

> 最後更新：2026-08-13
> 設計支柱唯一的家：[PILLARS_2.md](PILLARS_2.md)（現為 **v8**）
> 爬井 spike：[spike_well/](spike_well/)，專屬規則見 [spike_well/CLAUDE.md](spike_well/CLAUDE.md)，
> 偏離規格的現行規則見 [spike_well/.claude/docs/deviations.md](spike_well/.claude/docs/deviations.md)
> 歷史（發生過什麼）：[HANDOFF_ARCHIVE.md](HANDOFF_ARCHIVE.md)

---

## 當前狀態

spike_well 08-10 起**定位轉正**（籌備 itch.io 首發），治理文件已瘦身到 `.claude/docs/*.md`
＋ skill（細節見 archive「2026-08-10 治理調整」）。Git 備份：
`https://github.com/k082457230/RAORA-Basement.git`（使用者私人帳號）。

spike **v21 全綠**（六組稽核 ＋ 生成器稽核 ＋ bot 4 局；**各組項數以實跑輸出為準，不抄在這裡**）。
08-07～08-13 施工細節全搬進 [HANDOFF_ARCHIVE.md](HANDOFF_ARCHIVE.md)；偏離規格現行規則
唯一的家＝[deviations.md](spike_well/.claude/docs/deviations.md)。

08-10 前段四項已真人試玩確認 OK；**08-10 後段～08-13（含四訂）全部只過機器稽核與截圖，
尚未真人試玩**。

**08-13 二訂**：真人試玩回報後修正三件事——破關解鎖門檻各提前一關
（通關一→手套＋極限、通關二→懷錶＋無盡，對齊劇情播出時機）、jetpack 點火新增石化加速、
平台四態貼圖分組修正（會動三種共用 move.png、EXPLOSIVE 未觸發前跟 STATIC 同色）。細節見
archive 同名條目。

**08-13 三訂** —— 驗證體系擴充：`record.tscn` 決定性錄影、`audit_ui.gd` 通用版面掃描、
`tools/measure_anchor.py` 錨點量測腳本化。詳見 archive 同名條目。

**最新（08-13 四訂，本 session）—— 使用者五項**：①**教學關上線**（開幕劇情後強制一次，200m 出口，
固定佈局＋字卡＋固定高度真觸發怪物與干擾，只有金幣入帳，通關後永久移除）②**死亡文字與結算卡改版**
（卡片砍到大字＋高度＋用時＋金幣；死因對應句表＝使用者的 `dead.txt`）③**井底屍體堆**（關卡×模式
各自累計）④**石化改規格**（jetpack 噴射期間持續加轉速，取代二訂的點火加一次）⑤**工作人員名單佔位頁**。
教學關由 sonnet subagent 實作、主線重跑驗證並補修兩處（字卡按鍵寫死、教學關顯示無意義的登場倒數）。
現行規則全在 [deviations.md](spike_well/.claude/docs/deviations.md)，沿革與坑見 archive 同名條目。

## ▶ 下個 Session 起點

**⚠ 三訂留下、仍待你拍板的兩件**（08-13 四訂沒動到）：

1. **bot 的鞭子週期**：`bot_run.gd` 起手週期 6 秒 > bot 實際存活 3~4 秒 ⇒ smoke 四局的鞭子路徑
   **從來沒被真的執行過**（輸出全是「射出 0／命中 0」）。要不要比照 `record.gd` 縮到 1.5 秒？
   改了會變動既有稽核的意義，所以本輪**刻意沒動**。
2. **錄影要不要接 HUD／UI 層**：目前 `record.tscn` 只建 `WellWorld`，左下角 HUD（動態由下往上疊、
   最容易出版面問題的地方）完全沒入鏡。要接得走 main.gd 流程，複雜度高一階。

**⚠ 等使用者補素材**（三批，全部到位前相關畫面都是佔位）：
1. 九張 **112×112** PNG（八種 buff ＋ 懷錶），主體畫滿畫布、四周透明留白 ≤4px（金幣那組
   因大片留白吃過虧）。到位後走 skill `/import-art-asset`，目標視覺尺寸：場上球 56×56
   （`BUFF_ORB_ART_SIZE`）、左下角格子 48×48（`HUD_CELL_SIZE`）、右上角 toggle 56×56。
2. **滿版劇情圖**（項目 9）：`SpikeUI._story_art` 那塊 ColorRect 換成滿版 TextureRect 即可，
   文字換 `LEVEL_STORY_PLACEHOLDER`／`STORY_INTRO_PLACEHOLDER`。
3. **解鎖卡 icon**（項目 10）：`_unlock_glyph` 那顆 Label 換 TextureRect，版面不動。

**這次真人試玩重點**：

-2. **08-13 四訂（本輪五項，全部只過機器稽核與截圖）**：
   ①**教學關整條**最重要——十個教學點的順序讀不讀得懂、字卡位置會不會擋到落點、鞭子段與
   jetpack 段是不是真的「非用不可」而不是卡死、四種干擾示範的登場時機會不會嚇到新手、
   200m 會不會太長（教學關預期要玩幾分鐘？）②死亡結算卡的死因文字對不對得上當下的死法、
   NEW 標記看不看得到 ③井底屍體堆：死幾次之後開始有「屍山」的感覺、40 具會不會太擠
   ④石化＋jetpack 持續加速：長按噴射轉到 5 圈/秒會不會暈、鬆手後緩下來的曲線有沒有感覺
   ⑤設定頁「工作人員名單」按鈕位置。

-1. **08-13 十三項＋二訂**：①左下角 HUD 讀不讀得懂 ②石化轉速「越彈越快再緩下來」有感覺嗎、
   5 圈/秒會不會暈 ③第三關 1000m 第二組三選一位置與雙 buff 手感 ④視野縮小亮圈 180px 夠不夠
   看到落點 ⑤鞭中怪物暈眩後「撞死牠」順不順 ⑥登場震動 2 秒會不會太久 ⑦劇情／解鎖蒙版節奏
   ⑧二訂：解鎖門檻對不對（一→手套＋極限、二→懷錶＋無盡）、jetpack 點火轉速有感覺嗎、
   爆炸平台觸發前跟一般平台看不看得出差異。

0. **08-11 六項**：子彈變快躲不躲得掉、solo 隔一塊夠不夠喘、平台鏡像重複感、踩踏晃動幅度、
   蟲洞逆光搶不搶眼、下墜 steady 姿勢讀不讀得出來。
1. **solo 區間還卡不卡**（最重要，落腳窗只剩 0.5px 餘裕）：690m 以上「平台上有怪」時踩得到
   邊緣往上跳嗎？相關常數已無調整空間，真的太緊 ⇒ 下一步是「solo band 有怪就保底多生一塊
   乾淨跳板」（刻意還沒做，會削壓迫感，留到真的卡死再做）。
2. **平台四態貼圖**（08-10 七訂，08-13 二訂改分組）：像不像同一組材質、會動三種共用
   move.png 靠顏色分不分得出方向、EXPLOSIVE 未觸發前跟 STATIC 同色會不會太難察覺、
   終點貼磚好不好看、拿掉加寬後手感有沒有變化。
3. **08-10 後段～六訂全部**（見 archive）：jetpack 冷卻／無敵窗、Pameloe 雷射與鏡像、
   COIN／FUEL 漂浮、開發者傳送記錄、爆炸平台引信與半徑、主題區讀不讀得出來。
4. 舊帳未還：爬過 1000m 後的斷層感、側風 3000m 轉折、關卡二／三實際爬完——從 08-10 前段
   留到現在。高處可用右緣 `▲ +300 m` 直接跳，會**正常記錄**（不算作弊局）。

> 🔎 使用者觀測（下個 session 開頭補上）：______

---

## 常青認知（跨 session 都成立，動對應系統前先讀）

1. **「大小 ×2」要連判定一起乘**（v12）——只放大視覺等於偷偷把判定變寬鬆。
2. **淡出必須在畫面內演完**（v13）——時長算到超出畫面底才算數，否則等於白做。
3. **衝擊波是累加常駐的獨立速度分量**（v11），三種干擾解鎖後不互斥，`Interference.stage()`
   用 `>=` 判斷。
4. **稽核容易騙過自己**（v10/v17）：邊玩邊生成／回收的東西要模擬真實路徑（蟲洞曾因串流
   prune 早於出口綁定而機率為零）；稽核間會互相污染狀態，每條自己定死依賴狀態。
5. **階梯成就三階共用同一張卡片版位**（v15）：`SpikeSave.current_tier_id(slot)` 動態決定，
   不能在卡片建構時把 id 綁死。
6. **稽核本身也可能是壞的**（v16）：「保命條款」型常數要另寫一條驗常數關係的斷言（範本
   `tests/audit_hazards.gd` `_audit_pameloe` 的 `consts_ok`），新稽核要用突變測試驗抓得到。
7. **死亡不是同一幀的事**（v17）。`_die()` 只起爆，`died` 要等爆炸演完（`_settle_death`）
   才 emit，只呼叫判定會拿到靜默假陰性。
8. **貼圖繪製常見坑**（v18，接新素材會再遇到）：`modulate` 是乘法不是取代，染色前先做 RGB
   全白剪影（`_make_silhouette`）；`draw_texture_rect` 負寬度即原地鏡像不用補 `+size.x`；
   鏡像方向要實測不用猜；**站在平台上的東西**用腳底錨點（懸浮物例外），**平台本身**用
   頂部錨點；輪廓描邊要畫在本體之前（`_draw_sprite_outline`）。
9. **生成鏈用「上一塊的高度」決定「這一塊的性質」**（v18）：`_generate_next` 的 `h_m` 是
   上一塊的，高度分區判斷會因此差一格，稽核不准拿高度反推分區，改存旗標
   （`WellPlatform.segment_id`）。
10. **同 y 的兩塊板不能靠排序認人**（v19，08-11）：`sort_custom` 不保證穩定 ⇒「群裡最高
   那塊＝主鏈」的假設會偶發失效，跟第 9 條同教訓：**身分要存旗標**（`is_band_extra`），
   不要從幾何反推。
11b. **共用的「純函式」可能偷偷在骰 RNG**（v19，08-12）——`WellGenerator.spacing_at()`
   看起來只是查表插值，內部卻有一行 `_rng.randf_range()`，固定佈局路徑呼叫到它會讓生成
   序列偏移、**既有固定 seed 稽核照樣全綠**。⚠ 新路徑要先確認 helper 沒碰 `_rng`，並補
   「同 seed 下主 RNG 下一個輸出不變」的稽核。
12. **新的 `class_name` 檔要先跑一次 `--import` 才註冊得到**（v20，08-13）——直接跑
    `res://smoke.tscn` 會是 `Could not find type "X" in the current scope`，看起來像打錯字。
13. **UI 的兩個坑**（v20，08-13，只有 `visual_check` 截圖看得出來）：①`set_anchors_preset`
    之後 `position` 是相對**錨點**算的，BOTTOM_LEFT ＋ 正 y 會把子節點推到框外 ②「畫 N 層
    同心環近似漸層」重疊處 alpha 疊兩次會看到接痕，改用 `GradientTexture2D`（FILL_RADIAL）。
14. **紅燈先懷疑斷言**（v20，08-13）：「不污染主 RNG」第一版比錯對象（兩座井爬到同一高度
    後的下一個亂數，本來就會不同），正確比法是 `_rng.state` 前後不變。
11. **`process_mode` 沒設就是繼承父節點**（v17 續），不是獨立生效 PAUSABLE——`Main` 底下掛
   新節點沒明確設就會悄悄繼承 `ALWAYS`，`get_tree().paused` 對它沒用。
16. **新增一種「模式」時，要把所有時間驅動的 UI 與所有存檔寫入各盤點一次**（v21，08-13 四訂）——
    教學關把干擾改成高度觸發，但右上角的「Raora 登場倒數」照著 elapsed 跑，67 秒後印出
    「Raora 已登場」而實際什麼都不會發生。這種 bug 不會報錯、稽核也不會紅（每個元件單獨看都正常），
    只有真的把新模式從頭玩一遍才看得到。同理要盤點「這個模式該不該寫進存檔」的每一個呼叫點
    （最高高度／成就／通關紀錄／解鎖／屍體堆／遊玩次數／run seed，教學關七個全部要跳過）。

17. **玩家可見文案裡不准寫死按鍵，也不准寫死吃升級的數字**（v21，08-13 四訂）——
    「按 E 甩鞭子」在玩家改鍵之後就是**在教學玩家按錯鍵**，而且不報錯；「鞭子有 5 次」會被永久
    升級改掉。一律整句模板 ＋ 展開函式（`SpikeConfig.tutorial_cue_text()` 走 `SpikeKeys.label_of()`），
    這也正是 i18n 條款要求的寫法。

15. **headless 模擬按鍵，`Input.parse_input_event()` 不會立刻生效**（v20，08-13 二訂）：
    要接著呼叫 `Input.flush_buffered_events()` 才會反映在 `is_key_pressed()`，否則稽核會
    誤判「這條路徑沒接上」。

---

## spike 執行方式

**唯一的家＝[spike_well/CLAUDE.md](spike_well/CLAUDE.md)「執行方式」**（跑法、回歸測試指令、
cp950／visual_check／Win32 點擊三個坑、config 與 smoke 怎麼省 token 讀）。這裡不複製。

---

## 🔜 itch.io 試玩發佈

程式面已備妥（字型內嵌、標題、`stretch=canvas_items/keep`）。**只能在編輯器手動操作**。
已完成（08-07 驗證）：Export Templates 已裝、Web preset 四項設定到位、Run in Browser 冒煙過關。

**等修得差不多再做**（每改一次程式就作廢）：zip 打包／itch 頁面設定（Kind=HTML、
Viewport 1280×720、勾 Fullscreen）／存檔流失提醒文案（遊戲內已有匯出碼可指引）。

**改文案去哪改**（08-13 四訂改組過）：死因對應的**大字** → `SpikeConfig` 的 `DEATH_LINE_*`
（哪個死因配哪一句＝`WellWorld.death_line()`）；`CAUSE_*` 那組字串現在只是判定用的 id，
不再直接印給玩家。結算卡版面 → `spike_ui.gd` `set_result()`；教學關字卡 →
`SpikeConfig.TUTORIAL_CUE_CARDS`（按鍵一律寫 `{aim}` 模板，別寫死）；按鈕／商店文案 → `spike_ui.gd`。

---

## 未動工但已有定論（**不要重新調查一次**）

1. **資源消耗**：健康，⚠ 不加物件池（零 `queue_free`，實體皆 `RefCounted` 純資料）。
2. **存檔相容性**：⚠ 仍未解的三個雷：改 key 名或刪 key、改欄位語意、**改 Godot 專案名稱**
   （＝全體玩家存檔消失，要列進發佈檢查清單）。08-13 已知情踩過第一個雷（升級表刪 `ledge`，
   舊檔直接忽略），影響已評估，見 archive 08-13。
3. **榜單**：前置三項已做；後端與防作弊**等無盡難度曲線定案後再投**——現在做等於錨死一份
   很可能要重寫的 schema（主欄位屆時會從「最快登頂時間」換成「最高高度」）。`levels`
   永久升級已拍板**不分組**。
4. **i18n（中／英／日／印尼）**：字型已備；key 化／CSV **等手感定案後一次做完**。⚠ 現在就
   要守：文案一律整句模板 ＋ `format()`，禁止字串拼接（日／印尼文語序不同）。

---

## Deferred（未決清單）

完整清單見 [PILLARS_2.md](PILLARS_2.md)「未定案 → 仍未決」。摘要：

1. **待辦**：`PILLARS_2.md`「干擾升壓到必然墜落」那句要改寫成「高度足夠後必然墜落」（側風
   3000m 觸頂後才成立，1000m 關仍是純技術挑戰；已拍板實作，細節見上方第 3 條）。
1b. ⚠ **主題區備援跳板會機率性擺不下**（正式待辦，**不要再靠換 seed 續命**，已換兩次不准
   再換）：25 顆 seed 有 6 顆撞上（≈24% 局裡至少一個主題區高度區間沒備援板，純運氣牆）。
   根因是備援板水平視窗被主鏈 MOVING 板運動包絡線吃光（`_pick_x_apart` 的 ⚠⚠）。可能修法
   （未拍板）：擺不下時該 band 主鏈降回 STATIC／收窄巡邏半徑／允許備援板落可達視窗外緣。
2. 干擾期存活時長、探索死亡機率與產出規則、徵收比例是否綁梯度（舊基準過時，需按 20% 重算）
3. 生成器自動可行性檢查——需把「抽跳板後仍可解」納入判定
4. +75s buff 的生效綁定（建議自動綁下一次爬井）
5. **金幣經濟未平衡** — 掉落率與 5 項升級價格都是初值
6. **手機適配延後（統一電腦優先）**，守兩條可逆性條款：①不移除 `MOUSE_DRAG` 輸入模式
   （未來觸控地基）②新 UI 按鈕最小邊守 **44px**。⚠ 傾斜／陀螺儀控制已否決，別再提案。
7. **美術素材規範**：design resolution 1280×720、每物件 2 倍碰撞尺寸畫、Linear＋Mipmaps
   匯入；已匯入清單唯一的家是 `art-assets.md`（不重複抄）。⚠ **下一個要接的素材是死亡爆炸**
   （使用者說會補）：只換 `_draw_death_fx`；`_draw_blasts`（爆炸平台）不能共用同套畫法，
   外環不准隨演出縮放。

---

## 專案現況

- **階段**：pre-production。設計已收斂（v8）；爬井核心已有可玩原型；主專案仍無程式碼
- **文件**：`PILLARS_2.md`（支柱）、`HANDOFF.md`（本檔）、`HANDOFF_ARCHIVE.md`（歷史）、
  `spike_well/CLAUDE.md`（專屬規則）。**尚未建立** `PROJECT_MAP.md`
- **治理**：spike 不搬 grab2 治理包，跨專案底線靠全域 `~/.claude/CLAUDE.md`
