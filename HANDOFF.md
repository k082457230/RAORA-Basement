# HANDOFF — RAORA

> 最後更新：2026-08-10
> 設計支柱唯一的家：[PILLARS_2.md](PILLARS_2.md)（現為 **v8**）
> 爬井原型 spike：[spike_well/](spike_well/)，其專屬規則與偏離表見 [spike_well/CLAUDE.md](spike_well/CLAUDE.md)
> 歷史（發生過什麼）：[HANDOFF_ARCHIVE.md](HANDOFF_ARCHIVE.md)

---

## 當前狀態

spike **v18 全綠**（機制 20 項／UI 15 項／關卡 12 項／生成器稽核／bot 4 局）。08-09～08-10
施工細節全搬進 [HANDOFF_ARCHIVE.md](HANDOFF_ARCHIVE.md)，這裡不複述。

08-10 前段的四項修正（chattini 鏡像、踩頭判定框、Pameloe 充能圈、蟲洞金光）**已經真人
試玩確認過手感 OK**。本輪（08-10 後段）新做四件事，規則的家都在
[spike_well/CLAUDE.md](spike_well/CLAUDE.md) 偏離表同名列，這裡只列標題：

1. **solo 區間的怪物**——真人回報「高處平台上有怪＝一定得靠 jetpack 或鞭子」，根因是
   怪物的巡邏包絡線比平台還寬（幾何上無解）。縮巡邏 ＋ 加寬 solo 平台 ＋ chattini 減速 25%。
2. **COIN／FUEL 貼圖**（例外五）＋沿輪廓 +2px 白光＋三者漂浮。
3. **爆炸平台**（新平台種類，關卡二起）。
4. **特殊區段**（40m 主題區，首種是移動平台區）。

⚠⚠ 本輪**改掉了一條已拍板規則**：「關卡不改變高度→難度對應」由使用者推翻，改成
**白名單制**（`SpikeConfig.LEVEL_GATED`）。理由與新規則見偏離表「關卡」列。

08-10 三訂（本輪）又做了四件事，規則的家都在 `spike_well/CLAUDE.md` 偏離表同名列，
這裡只列標題，**全部只過了 headless 稽核與 visual_check 截圖，沒有人真的玩過**：

1. **COIN／FUEL 尺寸 -50%**：判定與畫面尺寸等比減半，`PICKUP_ART_SCALE` 新常數；
   `tests/audit_levels.gd` `_audit_pickup_art` 的比對基準同步改成「畫布 × 縮放」。
2. **Pameloe 開火鏡像**：開火瞬間依方向翻轉本體貼圖（子彈／雷射兩分支都會觸發）。
3. **Pameloe 雷射變體**：稀有立繪機率 10%→20%，抽到那張不再發子彈、改發持續 1s 的雷射，
   狀態掛在本體上（殺掉牠會立刻掐斷雷射，跟子彈脫鉤飛完刻意不同）。稽核在
   `tests/audit_hazards.gd` `_audit_pameloe` 新增四項。
4. **jetpack 無敵窗與冷卻**：噴射後的無敵餘韻從共用 0.5s 改成專屬 0.2s（鞭子／彈射板／
   蟲洞過場仍是 0.5s）；另加噴射結束後 0.6s 的強制冷卻（跟 0.2s 冷啟動是兩件事）。

---

08-10 五訂（本輪）四件事，**全部只過了 headless 稽核，沒有人真的玩過**：

1. **開發者傳送不再標記作弊局**：`▲ +300 m` 按過之後成績／成就／解關一律正常回報。
   理由：純測試用機制，只在 `dev_mode()` 為真時才建得出來，一般玩家碰不到、不會被濫用。
   `SpikeSave.cheated_run` 機制與六個寫入端的守衛整條移除，不留死代碼。
2. **Pameloe 雷射瞄準提前到充能起點鎖定**：舊版直到充能結束才決定方向，玩家整段充能
   看不到要打哪，等於沒有預警、幾乎必死。改成充能一開始就鎖方向＋畫瞄準虛線，充能的
   整段時間都看得到打哪、來得及躲；之後跟子彈同一條「鎖了不追蹤」規則不變。
3. **Pameloe 上下漂浮幅度 ×3**（5→15px）：`consts_ok` 的餘裕檢查仍過（71 > 54，見
   `spike_config.gd` `PAMELOE_FLOAT_AMP` 註解）。
4. **金幣／燃料懸浮不再卡進平台**：`PICKUP_HOVER` 13→22，兩者共用同一顆常數卻只照
   金幣的視覺尺寸算，漏看 fuel 比金幣高——現在改成蓋住較大那顆（fuel）＋2px 安全距。

## ▶ 下個 Session 起點

**這次真人試玩重點**：

0. **08-10 三訂四件事**（見上方「當前狀態」，同樣只過了機器稽核與截圖）：jetpack 冷卻
   0.6s／無敵窗 0.2s 手感會不會太緊（尤其是連續甩鞭+jetpack 的高處逃生橋段）；Pameloe
   雷射 1s 讀不讀得出來、站著不動被燒死的死法合不合理；Pameloe 開火鏡像有沒有跳動；
   COIN／FUEL 縮小後在正常遊玩距離看不看得清楚（visual_check 是近距離特寫，不等於
   實際爬井時的視距）。
0b. **08-10 五訂四件事**（見上方「當前狀態」，同樣只過了機器稽核，沒人玩過）：雷射瞄準
   虛線在充能期間看不看得清楚方向、有沒有真的來得及躲；Pameloe 漂浮 ×3 後晃不晃得出來
   （會不會反而晃到不像懸浮、像故障）；金幣／燃料的新懸停高度看起來自不自然；用
   `▲ +300 m` 跳關測試時留意成績有沒有正常記錄（可以拿來當「正常回報」的驗收）。
1. **solo 區間還卡不卡**（最重要，這是本輪的起因）：690m 以上遇到「平台上有怪」時，
   現在踩得到邊緣往上跳嗎？數字上兩側各有 27.5px 的落腳窗，但那是**幾何有解**，
   不等於**手感做得到**。⚠ 餘裕只比稽核下限多 3.5px，三顆常數（`PLATFORM_WIDTH_MULT_SOLO`
   ／`MONSTER_PATROL_RANGE_SOLO`／`MONSTER_SIZE`）已經沒有多少調整空間——真的還是太緊，
   優先動加寬倍率，不要動怪物判定框。若仍不行，第二道處方是「solo band 有怪就保底多生
   一塊乾淨跳板」（本輪刻意沒做，它會削掉 solo 區間的壓迫感）。
2. **爆炸平台**：2s 引信讀不讀得出來（亮度＝剩餘時間）？半徑 80 會不會追殺已經跳走的人
   （理論上不會，安全距 111）？無敵衝過去不死但爆炸消不掉，這條讀起來合不合理？
3. **主題區**：40m 的移動平台區進去之後感覺得出來是「一個區段」嗎，還是只覺得變難？
   怪物×2 ＋ 金幣×2 的取捨成不成立。
4. **金幣／燃料的漂浮與白光**：白光會不會太搶眼（比照蟲洞金光那次的回報）；漂浮只往上
   偏移，最低點離平台留 2px（08-10 五訂修正過卡進平台的問題），看起來會不會還是像
   「黏在板上」而不是漂浮。
5. 舊帳未還：爬過 1000m 之後的斷層感、側風 3000m 的轉折、關卡二／三實際爬完
   ——這三條從 08-10 前段留到現在，仍未試玩。高處可用右緣 `▲ +300 m` 直接跳
   （08-10 五訂起這局會**正常記錄**，見下方「當前狀態」）。

> 🔎 使用者觀測（下個 session 開頭補上）：______

---

## config 與冒煙測試怎麼讀（給下一個 agent）

- **改參數不要讀整檔**：讀 `spike_config.gd` 檔頭索引（前 30 行）→ `Grep "SECTION 4b —"` 定位
  → `Read` 帶 offset/limit。索引刻意不寫行號（行號一改就過期，過期的索引比沒有更糟）。
- **改稽核不要讀整包**：讀 `smoke.gd` 檔頭索引 → 開對應的 `tests/*.gd` → Grep 函式名。

---

## 常青認知（跨 session 都成立，動對應系統前先讀）

1. **「大小 ×2」要連判定一起乘**（v12）。只放大視覺會讓「判定寬鬆」反過來變欺騙。
2. **淡出必須在畫面內演完**（v13）。時長算到超出畫面底的那一刻，淡出等於白做。
3. **衝擊波（側風）是累加常駐的獨立速度分量**（v11），三種干擾解鎖後不會互斥，
   `Interference.stage()` 用 `>=` 判斷。
4. **稽核容易騙過自己**（v10／v17）：邊玩邊生成／回收的東西要模擬真實路徑，不能只驗「生完
   看起來對不對」（蟲洞曾因串流 prune 早於出口綁定而機率為零）；稽核之間也會互相污染狀態
   （黑洞稽核搬玩家到隨機平台，Pameloe 稽核沿用同位置偶發紅燈）——每條稽核自己定死依賴的
   狀態，⚠ 偶發假陰性比沒測還糟，會訓練人把紅燈當雜訊。
5. **階梯成就三階共用同一張卡片版位**（v15）：UI 用 `SpikeSave.current_tier_id(slot)`
   動態決定顯示哪一階，不能在卡片建構時把 id 綁死。
6. **稽核本身也可能是壞的**（v16）：跟被測程式共用的常數寫壞驗不出來，「保命條款」型常數
   要另寫一條驗常數關係的斷言（範本見 `tests/audit_hazards.gd` `_audit_pameloe` 的
   `consts_ok`）；新稽核寫完要用突變測試驗它抓得到，全綠不等於有效，⚠ 突變要改邏輯不能改常數。
7. **死亡不再是同一幀的事**（v17）。`_die()` 只起爆，`died` 要等爆炸演完才 emit。任何「撞到會不會
   死」的稽核都得先把演出跑完（`_settle_death`），只呼叫判定就問「死了沒」答案永遠是 false，
   而且是**靜默的假陰性**——致命判定整條壞掉也照樣全綠。
8. **貼圖繪製的三個坑**（v18，接死亡爆炸／其他角色貼圖會再遇到）：① `modulate` 是乘法不是
   取代，染純色前要先做「RGB 全白、保留 alpha」的剪影圖（`WellWorld._make_silhouette`）。
   ② `draw_texture_rect` 負寬度是「原地」鏡像，不要自己補 `+ size.x` 想「翻到另一邊」。
   ③ **鏡像方向要跟來源圖預設朝向對齊，不能猜**：kaela（08-09）、chattini（08-10）都猜反
   過一次，都靠真人試玩才抓到——新貼圖上線先用 `visual_check.tscn` 肉眼比對兩個朝向。
   ④ **站在平台上的東西一律用腳底錨點，不准中心對齊**：art 是碰撞尺寸的 2 倍，中心對齊會
   讓貼圖底邊沉到平台上緣下方、蓋掉平台。規則見 `spike_well/CLAUDE.md` 偏離表「貼圖底部
   錨點」列＋SOP 第 4 步。⚠ 懸浮物（Pameloe）用中心對齊——它沒有「腳底」這條基準線。
   ⑤ **視覺 2 倍於判定的代價會跟著錨點跑**：art 錨點跟判定框錨點是兩件事，改一個不會自動
   連動另一個——chattini 08-10 真的踩到（art 貼平台、判定框忘記跟著移，落到視覺下半部，
   真人試玩才發現「踩頭像陷進身體」）。換錨點時兩個一起想。
   ⑥ **輪廓描邊必須畫在本體之前**（kaela 無敵窗／蟲洞金光／Pameloe 充能圈三處共用
   `WellWorld._draw_sprite_outline`）：它靠本體蓋住中間、只露出外圈，畫在本體之後
   等於整隻被塗色。
9. **生成鏈用「上一塊的高度」決定「這一塊的性質」**（v18）。要先知道種類與擺幅才算得出
   這一塊的 y，是先有雞還先有蛋的順序，所以 `_generate_next` 裡的 `h_m` 是**上一塊**的。
   ⚠ 凡是「這塊板屬於哪個高度分區」的判斷（solo band、主題區）都會因此差一個高度區間，
   在分區邊界上漏掉防護。兩種修法：判斷得起的用新平台的高度重算（`monster_ok` 那條），
   算不出來的就**存旗標**（`WellPlatform.segment_id`）。⚠⚠ **稽核不准拿高度去反推分區**
   ——那會在每一段的頭尾各誤判一次，紅得毫無道理，而且看起來像被測程式壞了。
10. **`process_mode` 沒設就是繼承父節點**，不是獨立生效 PAUSABLE（v17 續）。`Main` 設
   `PROCESS_MODE_ALWAYS` 是為了讓暫停中 UI 仍可互動，但 `World` 一直沒明確設
   `PROCESS_MODE_PAUSABLE`，於是也跟著繼承 `ALWAYS`——`get_tree().paused = true` 對它
   完全沒用，遊戲在背後照跑。⚠ 往 `Main` 底下掛新節點，只要沒明確設 `process_mode` 就會
   悄悄繼承 `ALWAYS`，同一個坑會再犯。

---

## spike 執行方式

```
C:/Users/gnt0233/Downloads/Godot_v4.6.1-stable_win64.exe --path <repo>/spike_well
```
**回歸測試**（四組稽核 ＋ headless bot 4 局；各組項數以實跑輸出為準）：
```
C:/Users/gnt0233/Downloads/Godot_v4.6.1-stable_win64_console.exe --headless --path <repo>/spike_well res://smoke.tscn
```
⚠ Windows console 是 cp950，中文必亂碼但**資料本身正常**——導進檔案再讀回來驗，別重跑追亂碼。
⚠ **改過中文文案要重跑** `python spike_well/tools/subset_font.py`，**換字型檔還要多跑一次**
   `Godot ... --headless --path <spike> --import`，否則 `ResourceLoader.exists()` 會是
   false、UI 靜默退回 SystemFont。
⚠ 目視版面用**不加 `--headless` 跑 `res://visual_check.tscn`**（headless 下畫面是空白）；
   數字算得到的版面優先寫成稽核斷言。做法見 `spike_well/CLAUDE.md`「美術素材匯入 SOP」第 8 步。
⚠ 用 Win32 合成滑鼠點擊戳遊戲視窗**行不通**（hover 會亮但按鈕不觸發），別再花時間在那條路上。

---

## 🔜 itch.io 試玩發佈

程式面已備妥（字型內嵌、標題、`stretch=canvas_items/keep`）。**只能在編輯器手動操作**。
已完成（08-07 驗證）：Export Templates 已裝、Web preset 四項設定到位、Run in Browser 冒煙過關。

**等修得差不多再做**（每改一次程式就作廢）：zip 打包（根目錄放 `index.html`）／itch 頁面設定
（Kind=HTML、Viewport 1280×720、勾 Fullscreen）／存檔流失提醒文案（遊戲內已有匯出碼可指引）。

**改文案去哪改**：結算／死因文字 → `src/well_world.gd`（`died.emit` 三處）＋
`src/spike_ui.gd` `set_result()`；教學／按鈕／商店文案 → `src/spike_ui.gd`。

---

## 未動工但已有定論（**不要重新調查一次**）

1. ~~資源消耗~~ **結案**：健康，⚠ **不要加物件池**（零 `queue_free`，實體都是 `RefCounted` 純資料）。
2. ~~存檔相容性~~ **本輪做完**。⚠ 仍未解的三個雷：改 key 名或刪 key、改欄位語意、
   **改 Godot 專案名稱**（＝全體玩家存檔消失，要列進發佈檢查清單）。
3. ~~無盡加壓~~ **08-10 已實作完成**。公式與理由唯一的家：`spike_well/CLAUDE.md` 偏離表
   「終點」「關卡」「無盡模式」三列，這裡不重複抄一次。
4. **榜單**：前置三項（時間落盤／`extreme_mode` 拆欄位／RNG seed）已做；後端與防作弊
   **等無盡難度曲線定案後再投**——現在做等於錨死一份很可能要重寫的 schema（主欄位屆時會從
   「最快登頂時間」換成「最高高度」）。⚠ `levels` 永久升級直接改變跳躍與燃料，已拍板**不分組**。
5. **i18n（中／英／日／印尼）**：字型已備（Noto Sans CJK TC）；key 化／CSV 建置**等手感定案後
   一次做完**（現在做就是接下來每次改文案多兩道工序）。
   ⚠ **現在就要守**：新寫／改寫任何文案一律整句模板 ＋ `format()`，禁止字串拼接組句
   （日文／印尼文語序跟中文不同，拼接會壞）。

---

## Deferred（未決清單）

完整清單見 [PILLARS_2.md](PILLARS_2.md)「未定案 → 仍未決」。摘要：

1. **待辦**：`PILLARS_2.md`「干擾升壓到必然墜落」那句要改寫成「高度足夠後必然墜落」（側風
   3000m 觸頂後才成立，1000m 關仍是純技術挑戰；已拍板實作，細節見上方第 3 條）。
2. 干擾期存活時長、探索死亡機率與產出規則、徵收比例是否綁梯度（v8 註：舊基準已過時，需按 20% 重算）
3. 生成器的自動可行性檢查 — v7 約束：須把「抽跳板後仍可解」納入判定
4. +75s buff 的生效綁定（建議自動綁下一次爬井）
5. **金幣經濟未平衡** — 掉落率與 5 項升級價格都是初值
6. **手機適配延後（統一電腦優先）**，但要守兩條可逆性條款：① 不要移除 `MOUSE_DRAG` 輸入
   模式（未來觸控的地基）② 新 UI 按鈕最小邊守 **44px**。⚠ 傾斜／陀螺儀控制已否決（iOS 權限
   常拿不到＋精度太差），別再提案。
7. **美術素材規範**：design resolution 1280×720、每物件 2 倍碰撞尺寸畫、Linear＋Mipmaps
   匯入；已匯入清單與例外規則唯一的家是 `spike_well/CLAUDE.md` 硬規則 4（不重複抄）。
   ⚠ **下一個要接的素材是死亡爆炸**（使用者說會補）：只換 `WellWorld._draw_death_fx`，
   SECTION 6c 時長常數留著。⚠ 爆炸平台的爆炸（`_draw_blasts`）也是 placeholder，但它跟
   死亡爆炸**不能共用**同一套畫法：外環是致命半徑的視覺化，不准隨演出縮放（見偏離表）。
   `coin.png`／`fuel.png` 已於 08-10 接進來（例外五）。

---

## 專案現況

- **階段**：pre-production。設計已收斂（v8）；爬井核心已有可玩原型；主專案仍無程式碼、無專案地圖
- **文件**：`PILLARS_2.md`（支柱 v8）、`HANDOFF.md`（本檔）、`HANDOFF_ARCHIVE.md`（歷史）、
  `NOTEBOOK LLM (1).md`（原始構想）、`spike_well/CLAUDE.md`（spike 專屬規則＋刻意偏離表）。
  **尚未建立** `PROJECT_MAP.md` —— 所以「改文案去哪改」暫時只能住在本檔上面那一段
- **治理決定**：spike 不搬 grab2 治理包（避免第二份真相來源），跨專案底線靠全域 `~/.claude/CLAUDE.md`
