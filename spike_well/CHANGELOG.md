# CHANGELOG

> 格式參考 [Keep a Changelog](https://keepachangelog.com/)。版本號唯一來源＝
> `autoload/spike_config.gd` 的 `GAME_VERSION`，改版本號時同步在這裡開一個新區塊。
> 這是**對外**（玩家看得到的 devlog／頁面用）版本說明；內部施工細節仍住
> [../HANDOFF_ARCHIVE.md](../HANDOFF_ARCHIVE.md)，兩者用途不同、不要互相複製內容。

## [Unreleased]

尚未正式發佈 v1.0，本節暫時對應開發中狀態，首次上架時改名為對應版本號。

### Added
- 存檔新增 `game_version` 除錯欄位、寫檔改原子寫入、讀檔新增「存檔版本高於程式版本」防呆。
- 標題／工作人員名單頁新增非官方粉絲遊戲免責聲明（繁中）。
- 設定頁新增版本號顯示。
